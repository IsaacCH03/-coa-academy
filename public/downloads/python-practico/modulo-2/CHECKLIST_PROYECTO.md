# Lista de comprobación del proyecto

## Inventario

- [ ] Compruebo las longitudes de las cinco columnas.
- [ ] Detecto códigos duplicados.
- [ ] Valido precios y existencias.
- [ ] Utilizo `zip(..., strict=True)` después de validar.
- [ ] Construyo un diccionario indexado por código.

## Pedidos

- [ ] Conservo los pedidos originales.
- [ ] Detecto pedidos vacíos.
- [ ] Detecto prioridades no reconocidas.
- [ ] Detecto códigos desconocidos.
- [ ] Detecto cantidades que no son enteros positivos.
- [ ] Distingo `COMPLETO`, `FALTANTES` e `INVÁLIDO`.
- [ ] Calculo totales solo cuando es confiable.
- [ ] Registro todos los problemas encontrados.

## Herramientas

- [ ] Utilizo `enumerate()` con `start`.
- [ ] Utilizo comprensiones de lista, diccionario y conjunto.
- [ ] Utilizo una expresión generadora con `sum()`.
- [ ] Utilizo una expresión generadora con `any()` o `all()`.
- [ ] Utilizo `sorted()` con una regla clara.
- [ ] Utilizo `min()` y `max()` con `key`.
- [ ] Puedo justificar el uso de `lambda` o función con nombre.

## Entrega

- [ ] El reporte contiene tabla, resumen y detalle.
- [ ] Ejecuté los casos obligatorios y tres propios.
- [ ] Incluí `analizador_pedidos.py` y `datos_inventario.py`.
- [ ] Incluí README, plan de pruebas y evidencia.
- [ ] Respondí las preguntas de reflexión.
- [ ] El archivo comprimido tiene el nombre solicitado.
